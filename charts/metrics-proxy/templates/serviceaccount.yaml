apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "metrics-proxy.serviceAccountName" . }}
  labels:
    {{- include "metrics-proxy.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
